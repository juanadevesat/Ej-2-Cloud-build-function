steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - functions
  - deploy
  - pruebacloudbuild
  - --region=europe-west1
  - --source=./mi-app
  - --trigger-bucket=gs://prueba-cloudbuild-function
  - --runtime=python312
  - --allow-unauthenticated